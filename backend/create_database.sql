-- Create the database used by the backend
-- Run this in MySQL as a user with CREATE DATABASE rights.

CREATE DATABASE IF NOT EXISTS `online_voting_system`;
USE `online_voting_system`;

-- If you want to create a different database name, replace online_voting_system above.
